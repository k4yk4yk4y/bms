<%# app/views/layouts/_project_navigation.html.erb %>

<div class="project-dropdown">
  <a href="#" class="project-dropdown-toggle">Projects</a>
  <div class="project-dropdown-menu" style="max-height: 300px; overflow-y: auto;">
    <% Project.order(id: :desc).each do |project| %>
      <%= link_to project.name, admin_bonuses_path(project_id: project.id) %>
    <% end %>
  </div>
</div>
