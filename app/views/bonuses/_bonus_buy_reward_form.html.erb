<%
  # Set defaults for parameters
  reward ||= nil
  index ||= 0
  
  # Get existing bonus_buy reward for editing
  existing_bonus_buy = reward || (@bonus&.bonus_buy_rewards&.first if @bonus&.persisted?)
  
  # Field name prefix for indexed parameters
  field_prefix = "bonus_buy_rewards[#{index}]"
%>

<div class="card mb-3" id="bonus-buy-reward-<%= index %>">
  <div class="card-header d-flex justify-content-between align-items-center">
    <h6 class="mb-0">Bonus Buy Reward #<%= index + 1 %></h6>
    <button type="button" class="btn btn-sm btn-outline-danger" onclick="removeBonusBuyReward(<%= index %>)">
      <i class="fas fa-trash"></i> Remove
    </button>
  </div>
  <div class="card-body">

<!-- Основные часто используемые параметры для bonus_buy -->
<div class="main-params-container">
  <h6 class="text-warning reward-section-header">Основные параметры</h6>
  
  <!-- Buy Amount -->
  <div class="reward-param-field mb-3">
    <label for="bonus_buy_reward_buy_amount" class="form-label">Сумма покупки *</label>
    <div class="input-group">
      <input type="number" class="form-control" name="<%= field_prefix %>[buy_amount]" id="bonus_buy_reward_buy_amount_<%= index %>" 
             step="0.01" min="0" required  value="<%= existing_bonus_buy&.buy_amount %>">
      <span class="input-group-text">€</span>
    </div>
  </div>

  <!-- Multiplier -->
  <div class="reward-param-field mb-3">
    <label for="bonus_buy_reward_multiplier" class="form-label">Множитель покупки (multiplier)</label>
    <div class="input-group">
      <input type="number" class="form-control" name="<%= field_prefix %>[multiplier]" id="bonus_buy_reward_multiplier_<%= index %>" 
             step="0.01" min="0"  value="<%= existing_bonus_buy&.multiplier %>">
      <span class="input-group-text">x</span>
    </div>
  </div>

  <!-- Games -->
  <div class="reward-param-field mb-3">
    <label for="bonus_buy_reward_games" class="form-label">Игры (games)</label>
    <input type="text" class="form-control" name="bonus_buy_reward[games]" id="bonus_buy_reward_games_<%= index %>" 
            value="<%= existing_bonus_buy&.games&.join(', ') %>">
  </div>

  <!-- Bet Level -->
  <div class="reward-param-field mb-3">
    <label for="bonus_buy_reward_bet_level" class="form-label">Уровень ставки (bet_level)</label>
    <div class="input-group">
      <input type="number" class="form-control" name="bonus_buy_reward[bet_level]" id="bonus_buy_reward_bet_level" 
             step="0.01" min="0"  value="<%= existing_bonus_buy&.bet_level %>">
      <span class="input-group-text">€</span>
    </div>
  </div>

  <!-- Max Win -->
  <div class="reward-param-field mb-3">
    <label class="form-label">Максимальный выигрыш (max_win)</label>
    <div class="input-group">
      <input type="text" class="form-control" name="bonus_buy_reward[max_win]" id="bonus_buy_reward_max_win" 
              value="<%= existing_bonus_buy&.max_win %>">
      <span class="input-group-text">€ или x</span>
    </div>
  </div>

  <!-- Available -->
  <div class="reward-param-field mb-3">
    <label for="bonus_buy_reward_available" class="form-label">Доступно (available)</label>
    <input type="number" class="form-control" name="bonus_buy_reward[available]" id="bonus_buy_reward_available" 
           min="0"  value="<%= existing_bonus_buy&.available %>">
  </div>

  <!-- Code -->
  <div class="reward-param-field mb-3">
    <label for="bonus_buy_reward_code" class="form-label">Код активации (code)</label>
    <input type="text" class="form-control" name="bonus_buy_reward[code]" id="bonus_buy_reward_code" 
            value="<%= existing_bonus_buy&.code %>">
  </div>

  <!-- Stag -->
  <div class="reward-param-field mb-3">
      <label for="bonus_buy_reward_stag" class="form-label">Stag</label>
      <input type="text" class="form-control" name="bonus_buy_reward[stag]" id="bonus_buy_reward_stag" 
              value="<%= existing_bonus_buy&.stag %>">

    </div>

    <!-- Note: Currencies, Groups, Tags, Minimum Deposit, and Wagering Strategy are now configured in Basic Information -->
  </div>
</div>

<!-- Дополнительные параметры -->
<div class="mt-4">
  <button type="button" class="btn btn-outline-secondary btn-sm" id="toggle_bonus_buy_advanced" 
          onclick="toggleAdvancedBonusBuyParams()">
    <i class="fas fa-plus"></i> Показать дополнительные параметры
  </button>
  
  <div id="bonus_buy_advanced_params" class="collapse mt-3">
    <div class="card">
      <div class="card-header">
        <h6 class="card-title mb-0">Дополнительные параметры</h6>
      </div>
      <div class="card-body">
        <div class="advanced-params-container">
          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_auto_activate" class="form-label">Автоактивация</label>
            <select class="form-select" name="bonus_buy_reward[auto_activate]" id="bonus_buy_reward_auto_activate">
              <option value="">Не задано</option>
              <option value="true" <%= 'selected' if existing_bonus_buy&.get_advanced_param('auto_activate') == true %>>Да</option>
              <option value="false" <%= 'selected' if existing_bonus_buy&.get_advanced_param('auto_activate') == false %>>Нет</option>
            </select>
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_duration" class="form-label">Длительность</label>
            <input type="text" class="form-control" name="bonus_buy_reward[duration]" 
                   id="bonus_buy_reward_duration"  
                   value="<%= existing_bonus_buy&.get_advanced_param('duration') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_activation_duration" class="form-label">Время активации</label>
            <input type="text" class="form-control" name="bonus_buy_reward[activation_duration]" 
                   id="bonus_buy_reward_activation_duration"  
                   value="<%= existing_bonus_buy&.get_advanced_param('activation_duration') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_email_template" class="form-label">Email шаблон</label>
            <input type="text" class="form-control" name="bonus_buy_reward[email_template]" 
                   id="bonus_buy_reward_email_template"  
                   value="<%= existing_bonus_buy&.get_advanced_param('email_template') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_range" class="form-label">Диапазон (range)</label>
            <input type="text" class="form-control" name="bonus_buy_reward[range]" 
                   id="bonus_buy_reward_range"  
                   value="<%= existing_bonus_buy&.get_advanced_param('range') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_last_login_country" class="form-label">Страна последнего входа</label>
            <input type="text" class="form-control" name="bonus_buy_reward[last_login_country]" 
                   id="bonus_buy_reward_last_login_country"  
                   value="<%= existing_bonus_buy&.get_advanced_param('last_login_country') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_profile_country" class="form-label">Страна профиля</label>
            <input type="text" class="form-control" name="bonus_buy_reward[profile_country]" 
                   id="bonus_buy_reward_profile_country"  
                   value="<%= existing_bonus_buy&.get_advanced_param('profile_country') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_current_ip_country" class="form-label">Страна текущего IP</label>
            <input type="text" class="form-control" name="bonus_buy_reward[current_ip_country]" 
                   id="bonus_buy_reward_current_ip_country"  
                   value="<%= existing_bonus_buy&.get_advanced_param('current_ip_country') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_emails" class="form-label">Email адреса</label>
            <input type="text" class="form-control" name="bonus_buy_reward[emails]" 
                   id="bonus_buy_reward_emails"  
                   value="<%= existing_bonus_buy&.get_advanced_param('emails') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_deposit_payment_systems" class="form-label">Платежные системы депозита</label>
            <input type="text" class="form-control" name="bonus_buy_reward[deposit_payment_systems]" 
                   id="bonus_buy_reward_deposit_payment_systems"  
                   value="<%= existing_bonus_buy&.get_advanced_param('deposit_payment_systems') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_cashout_payment_systems" class="form-label">Платежные системы вывода</label>
            <input type="text" class="form-control" name="bonus_buy_reward[cashout_payment_systems]" 
                   id="bonus_buy_reward_cashout_payment_systems"  
                   value="<%= existing_bonus_buy&.get_advanced_param('cashout_payment_systems') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_user_can_have_duplicates" class="form-label">Дубликаты пользователям</label>
            <select class="form-select" name="bonus_buy_reward[user_can_have_duplicates]" id="bonus_buy_reward_user_can_have_duplicates">
              <option value="">Не задано</option>
              <option value="true" <%= 'selected' if existing_bonus_buy&.get_advanced_param('user_can_have_duplicates') == true %>>Да</option>
              <option value="false" <%= 'selected' if existing_bonus_buy&.get_advanced_param('user_can_have_duplicates') == false %>>Нет</option>
            </select>
            </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_user_can_have_disposable_email" class="form-label">Одноразовые email</label>
            <select class="form-select" name="bonus_buy_reward[user_can_have_disposable_email]" id="bonus_buy_reward_user_can_have_disposable_email">
              <option value="">Не задано</option>
              <option value="true" <%= 'selected' if existing_bonus_buy&.get_advanced_param('user_can_have_disposable_email') == true %>>Разрешить</option>
              <option value="false" <%= 'selected' if existing_bonus_buy&.get_advanced_param('user_can_have_disposable_email') == false %>>Запретить</option>
            </select>
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_total_deposits" class="form-label">Общие депозиты</label>
            <input type="number" class="form-control" name="bonus_buy_reward[total_deposits]" 
                   id="bonus_buy_reward_total_deposits" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('total_deposits') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_deposits_sum" class="form-label">Сумма депозитов</label>
            <input type="number" class="form-control" name="bonus_buy_reward[deposits_sum]" 
                   id="bonus_buy_reward_deposits_sum" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('deposits_sum') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_loss_sum" class="form-label">Сумма проигрышей</label>
            <input type="number" class="form-control" name="bonus_buy_reward[loss_sum]" 
                   id="bonus_buy_reward_loss_sum" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('loss_sum') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_deposits_count" class="form-label">Количество депозитов</label>
            <input type="number" class="form-control" name="bonus_buy_reward[deposits_count]" 
                   id="bonus_buy_reward_deposits_count" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('deposits_count') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_spend_sum" class="form-label">Сумма трат</label>
            <input type="number" class="form-control" name="bonus_buy_reward[spend_sum]" 
                   id="bonus_buy_reward_spend_sum" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('spend_sum') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_category_loss_sum" class="form-label">Проигрыши по категории</label>
            <input type="number" class="form-control" name="bonus_buy_reward[category_loss_sum]" 
                   id="bonus_buy_reward_category_loss_sum" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('category_loss_sum') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_wager_sum" class="form-label">Сумма вагера</label>
            <input type="number" class="form-control" name="bonus_buy_reward[wager_sum]" 
                   id="bonus_buy_reward_wager_sum" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('wager_sum') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_bets_count" class="form-label">Количество ставок</label>
            <input type="number" class="form-control" name="bonus_buy_reward[bets_count]" 
                   id="bonus_buy_reward_bets_count" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('bets_count') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_affiliates_user" class="form-label">Пользователь партнера</label>
            <input type="text" class="form-control" name="bonus_buy_reward[affiliates_user]" 
                   id="bonus_buy_reward_affiliates_user" 
                   value="<%= existing_bonus_buy&.get_advanced_param('affiliates_user') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_balance" class="form-label">Баланс</label>
            <input type="number" class="form-control" name="bonus_buy_reward[balance]" 
                   id="bonus_buy_reward_balance" step="0.01" 
                   value="<%= existing_bonus_buy&.get_advanced_param('balance') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_chargeable_comp_points" class="form-label">Списываемые комп-поинты</label>
            <input type="number" class="form-control" name="bonus_buy_reward[chargeable_comp_points]" 
                   id="bonus_buy_reward_chargeable_comp_points" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('chargeable_comp_points') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_persistent_comp_points" class="form-label">Постоянные комп-поинты</label>
            <input type="number" class="form-control" name="bonus_buy_reward[persistent_comp_points]" 
                   id="bonus_buy_reward_persistent_comp_points" step="0.01" min="0" 
                   value="<%= existing_bonus_buy&.get_advanced_param('persistent_comp_points') %>">
          </div>

          <div class="reward-param-field mb-3">
            <label for="bonus_buy_reward_date_of_birth" class="form-label">Дата рождения</label>
            <input type="date" class="form-control" name="bonus_buy_reward[date_of_birth]" 
                   id="bonus_buy_reward_date_of_birth" 
                     value="<%= existing_bonus_buy&.get_advanced_param('date_of_birth') %>">
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_deposit" class="form-label">Депозит</label>
              <input type="number" class="form-control" name="bonus_buy_reward[deposit]" 
                     id="bonus_buy_reward_deposit" step="0.01" min="0" 
                     value="<%= existing_bonus_buy&.get_advanced_param('deposit') %>">
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_gender" class="form-label">Пол</label>
              <select class="form-select" name="bonus_buy_reward[gender]" id="bonus_buy_reward_gender">
                <option value="">Не задано</option>
                <option value="male" <%= 'selected' if existing_bonus_buy&.get_advanced_param('gender') == 'male' %>>Мужской</option>
                <option value="female" <%= 'selected' if existing_bonus_buy&.get_advanced_param('gender') == 'female' %>>Женский</option>
              </select>
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_issued_bonus" class="form-label">Выданный бонус</label>
              <input type="text" class="form-control" name="bonus_buy_reward[issued_bonus]" 
                     id="bonus_buy_reward_issued_bonus" 
                     value="<%= existing_bonus_buy&.get_advanced_param('issued_bonus') %>">
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_registered" class="form-label">Зарегистрирован</label>
              <input type="date" class="form-control" name="bonus_buy_reward[registered]" 
                     id="bonus_buy_reward_registered" 
                     value="<%= existing_bonus_buy&.get_advanced_param('registered') %>">
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_social_networks" class="form-label">Социальные сети</label>
              <input type="text" class="form-control" name="bonus_buy_reward[social_networks]" 
                     id="bonus_buy_reward_social_networks"  
                     value="<%= existing_bonus_buy&.get_advanced_param('social_networks') %>">
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_wager_done" class="form-label">Отыгрыш выполнен</label>
              <select class="form-select" name="bonus_buy_reward[wager_done]" id="bonus_buy_reward_wager_done">
                <option value="">Не задано</option>
                <option value="true" <%= 'selected' if existing_bonus_buy&.get_advanced_param('wager_done') == true %>>Да</option>
                <option value="false" <%= 'selected' if existing_bonus_buy&.get_advanced_param('wager_done') == false %>>Нет</option>
              </select>
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_hold_min" class="form-label">Hold min</label>
              <input type="number" class="form-control" name="bonus_buy_reward[hold_min]" 
                     id="bonus_buy_reward_hold_min" step="0.01" min="0" 
                     value="<%= existing_bonus_buy&.get_advanced_param('hold_min') %>">
            </div>

            <div class="mb-3">
              <label for="bonus_buy_reward_hold_max" class="form-label">Hold max</label>
              <input type="number" class="form-control" name="bonus_buy_reward[hold_max]" 
                     id="bonus_buy_reward_hold_max" step="0.01" min="0" 
                     value="<%= existing_bonus_buy&.get_advanced_param('hold_max') %>">
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
function toggleAdvancedBonusBuyParams() {
  const advancedSection = document.getElementById('bonus_buy_advanced_params');
  const toggleButton = document.getElementById('toggle_bonus_buy_advanced');
  
  if (advancedSection.classList.contains('show')) {
    advancedSection.classList.remove('show');
    toggleButton.innerHTML = '<i class="fas fa-plus"></i> Показать дополнительные параметры';
  } else {
    advancedSection.classList.add('show');
    toggleButton.innerHTML = '<i class="fas fa-minus"></i> Скрыть дополнительные параметры';
  }
}
</script>
  </div>
</div>
